## 1. 桌面收敛

- [x] 1.1 移除 Web 导出、下载插件和浏览器运行分支，检查引用与 Godot 导入。
- [x] 1.2 改用本地场景加载，保留取消和会话连续性，通过三校区传送与物理检查。

## 2. 服务与交付

- [x] 2.1 Go 和镜像移除 Web 文件依赖、更新票据地址返回与直连拓扑，通过 HTTP 与真实入场检查。
- [x] 2.2 更新 CI 和发布配置，仅导出桌面客户端及游戏服，检查实际导出包与双容器运行。
- [x] 2.3 更新 AGENTS.md 和应用技术文档，记录 ENet 决策与 DTLS 部署配置、OpenSpec 严格校验与提交，保持根 README 不变。

## 3. ENet 迁移验证

- [x] 3.1 ENet 控制/实时通道、二进制快照、认证与切图协议替换；验证重放拒绝、满员、同身份替换、旧代次、输入伪造和 50 人快照。
- [x] 3.2 验证 150 ms RTT、丢包乱序下移动纠正，以及 Go HTTP 故障/状态重启期间连接持续和在线恢复。
- [x] 3.3 验证生产 DTLS 成功入场、不可信证书及主机名不符拒绝，并检查客户端和服务端资源隔离。

## 4. 默认渲染器

- [x] 4.1 切换 Forward+ 并移除移动端覆盖，同步约定，验证实际渲染及桌面/服务器导出。

## 5. 编辑器环境切换

- [x] 5.1 增加 Local / Dev 工具栏、本机持久选择、运行进程配置固定和显式变量覆盖提示，同步使用说明。
- [x] 5.2 验证编辑器交互、F5/F6 配置、重启保存、运行中隔离与显式覆盖，完成桌面和服务器导出、资源清单及 OpenSpec 校验并提交。

- [x] 5.3 将原合并插件整理到 addons/desktop_export 并改用 Env 工具栏文案（后续由 5.4 拆分职责），更新注册及引用并验证导入和导出隔离。

- [x] 5.4 将运行环境 UI 与桌面导出拆为两个独立插件，共用配置解析，验证独立启用、配置保存、导出清单和规范并提交。

## 6. CI 测试整理

- [x] 6.1 将 Go、Godot 和 Windows 凭据源码测试整合到单个 test 任务，移除构建前置专项测试和重复源码测试，同步规范。
- [x] 6.2 校验工作流语法和触发/依赖关系，运行可在本机执行的相关检查并提交。
