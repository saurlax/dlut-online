## 1. 协议与服务端

- [x] 1.1 扩展共享可靠控制消息、发送结果和事件序号，升级 Go 票据接口和游戏协议版本为 5，校验账号身份、纯文本边界与请求序号。
- [x] 1.2 实现跨三校区的同服务器聊天广播，使用服务端显示名，向发送者回传同一消息；按账号单调时间执行 1000 ms 间隔并覆盖重连/连接替换。
- [x] 1.3 从真实在线身份变化生成加入/离开事件，处理认证失败、重复关闭、旧回调与连接替换，不将切图当作离线。

## 2. 客户端与原生界面

- [x] 2.1 在网络 Autoload 接收事件、维护最多 100 条记录并去重，切图保留、会话结束清空；实现客户端冷却及拒绝结果处理。
- [x] 2.2 在左下角实现白色世界消息、黄色进出提示、底部 24 px 留白、换行和小窗口约束，保持右下角既有状态布局。
- [x] 2.3 实现回车编辑/发送、冷却期间回车无效、草稿保留、Escape、输入法与移动/鼠标状态协调，并接入覆盖层和失焦生命周期。

## 3. 验证与交付

- [x] 3.1 进行有意义的协议与限流检查：首次发送、999/1000 ms、自然秒边界、拒绝不延长冷却、伪造身份/系统通知、非法及超长文本、重连绕过、重复事件。
- [x] 3.2 用两个真实账号本地联调同校区与跨校区广播、发送者单次回显、加入/正常离开/超时离开、失败准入、同账号替换、切图期间接收。
- [x] 3.3 实际桌面检查底部 padding、颜色/精确文案、长文本、窗口缩放、中文输入法、快速回车、暂停/失焦、地图与切图，确认不误移动或自动发送。
- [x] 3.4 检查中文字体子集，执行相关 Godot 导入、Windows x86_64/macOS universal 客户端与 Linux 游戏服导出；如实说明无法实机验证的平台，临时产物仅留 `.local/`。
- [x] 3.5 核对最终实现与规范，执行 OpenSpec 严格校验与 diff 检查，按明确文件清单提交实现，不混入无关文件。

平台验证边界：macOS Forward+ 渲染、引擎输入事件与 IME 保护分支已检查；操作系统中文候选人工确认、Windows 实机运行及未签名 macOS 应用直接启动未完成。三个平台导出与资源包检查通过，桌面签名、公证仍未配置。
