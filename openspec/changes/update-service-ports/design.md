## Context

双服务默认端口需按用户指定统一调整。

## Goals / Non-Goals

统一默认端口，不改变协议、登录流程或端口覆盖方式。

## Decisions

游戏服使用 UDP 1949；Go 使用 TCP 8415。客户端开发默认地址及游戏服内部 Go 地址同步，Compose 的宿主机和容器默认映射保持一致，CI 自定义测试端口保留，仅更新游戏容器目标端口。

## Risks / Trade-offs

已有部署若显式填写旧地址，需同步其环境变量与端口映射。
