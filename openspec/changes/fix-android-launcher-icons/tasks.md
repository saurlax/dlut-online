## 1. 修改
- [x] 1.1 核对生成脚本、导出配置与 Godot 自适应图标文档。
- [x] 1.2 调整普通图标底板与两个前景比例，重新生成 SVG。
- [x] 1.3 通过真机单变量对照定位资源包名不一致，同步资源表包名并重新对齐和签名。
- [x] 1.4 包名改写仅保留必要的格式保护，不维护额外资源测试与逐次图标引用检查。

## 2. 验证
- [x] 2.1 检查生成一致性、透明边距、安全区及圆形/圆角遮罩渲染。
- [x] 2.2 运行 OpenSpec 严格校验并检查提交范围。
- [x] 2.3 对 PR 构建 APK 执行新增资源表后处理，验证签名、对齐和图标关联，并在 Honor 500 Pro 真机验收。
- [ ] 2.4 推送后通过完整 CI 构建验证。

验证：生成器 `--check`、Godot 4.7.2 SVG 实际栅格化、透明边距与白底像素检查、圆形及圆角遮罩预览通过。含抗锯齿的前景最大半径为 100.86 像素，小于 132 像素安全圆。原 PR CI 构建通过，用户确认安装器图标正确；后续通过 USB 在 Honor 500 Pro（MagicOS 10.0.0.175，Android 16，桌面 17.0.20.200）确认方形问题，使用相同图标、仅改变资源包名的对照 APK 复现原因。修正真实游戏 APK 后，系统返回正常主题位图，桌面圆角和尺寸恢复，启动检查通过；临时对照应用已移除。资源表回归检查、真实 APK 图标关联、非资源表载荷逐项一致性、v2/v3 签名和 16 KiB ZIP 对齐检查通过。本机未从源码重新执行完整 Godot 导出，使用已有 CI APK 执行新增后处理；最终完整构建待 CI 验证。无建筑、室内或游戏逻辑变化，未重复桌面/服务器导出和校园物理检查。
