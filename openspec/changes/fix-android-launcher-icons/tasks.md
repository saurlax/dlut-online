## 1. 修改
- [x] 1.1 核对生成脚本、导出配置与 Godot 自适应图标文档。
- [x] 1.2 调整普通图标底板与两个前景比例，重新生成 SVG。

## 2. 验证
- [x] 2.1 检查生成一致性、透明边距、安全区及圆形/圆角遮罩渲染。
- [x] 2.2 运行 OpenSpec 严格校验并检查提交范围。
- [ ] 2.3 重新导出 APK 并在 Android 安装器和启动器验收。

验证：生成器 `--check`、Godot 4.7.2 SVG 实际栅格化、透明边距与白底像素检查、圆形及圆角遮罩预览、OpenSpec 严格校验和 `git diff --check` 通过。含抗锯齿的前景最大半径为 100.86 像素，小于 132 像素安全圆。本机缺少 Android 导出模板，未重新导出 APK 或进行真机安装器/启动器验收，尚不能确认设备实际选择的图标资源。无建筑、室内或游戏运行逻辑变化，未重复桌面/服务器导出和校园渲染检查。
