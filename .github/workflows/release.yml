name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: read
  packages: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Validate version tag
        env:
          TAG: ${{ github.ref_name }}
        run: |
          [[ "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z]+([.-][0-9A-Za-z]+)*)?$ ]] || {
            echo 'Expected vMAJOR.MINOR.PATCH or vMAJOR.MINOR.PATCH-prerelease'
            exit 1
          }
  web:
    needs: validate
    uses: ./.github/workflows/build-web.yml
    with:
      release: true
  game:
    needs: validate
    uses: ./.github/workflows/build-game.yml
    with:
      release: true
  release:
    needs: [web, game]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: client-windows-${{ github.sha }}
          path: dist/windows
      - uses: actions/download-artifact@v4
        with:
          name: client-macos-${{ github.sha }}
          path: dist/macos
      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
          TAG: ${{ github.ref_name }}
        run: |
          image="ghcr.io/${GH_REPO,,}"
          printf 'Go HTTP Docker: `%s`\nGodot game Docker: `%s`\n\nWindows x86_64 and macOS arm64 clients are attached. Windows EXE is unsigned. macOS DMG contains an ad-hoc signed app, without Developer ID signing or Apple notarization; Gatekeeper may block browser downloads.\n' "${image}-web:$TAG" "$image:$TAG" > notes.md
          if ! gh release view "$TAG" >/dev/null 2>&1; then
            args=(--verify-tag --title "$TAG" --notes-file notes.md)
            if [[ "$TAG" == *-* ]]; then
              args+=(--prerelease --latest=false)
            else
              args+=(--latest)
            fi
            gh release create "$TAG" "${args[@]}"
          fi
          gh release upload "$TAG" dist/windows/DLUT-Online-Windows.exe dist/macos/DLUT-Online-macOS.dmg --clobber
