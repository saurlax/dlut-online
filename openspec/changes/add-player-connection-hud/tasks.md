## 1. 账号与连接数据

- [x] 1.1 在账号会话中单独保留认证/刷新返回的 `username` 并在清理时重置，保持现有昵称用途；通过已有账号检查验证用户名与昵称不同、字段缺失、恢复登录和更换账号场景。
- [x] 1.2 核实当前 Godot ENet RTT 统计和首次可靠往返的有效性判断，提供每秒更新的连接 RTT 与状态；通过本地真实连接验证默认统计不被展示、空闲时持续测量且来源为当前游戏连接。
- [x] 1.3 实现超过 5 秒无有效消息的显示过期、断线清理及重连重新测量，同连接切图保留状态；通过网络本地专项检查验证消息中断、恢复及旧连接数据不泄漏，既有 15 秒断线与退避行为不变。

## 2. 右下角显示

- [x] 2.1 在现有 HUD 添加原生右下角两行只读控件，显示账号用户名和 RTT/状态，支持长名称省略、边缘留白和默认字间距；实际渲染检查普通/长用户名、窗口缩放及明暗校园背景，确认延迟单位完整。
- [x] 2.2 按规范实现延迟数值颜色与异常状态文案；用本地临时输入检查 99、100、199、200 ms 的边界和未知/异常状态，确认用户名颜色不变，临时脚本放入 `.local/`。
- [x] 2.3 接入登录、地图、传送和暂停可见性，保持鼠标忽略及无焦点；实际操作验证 M/Escape、地图拖动缩放、点击恢复、失焦和三校区传送，无重复控件或额外认证。

## 3. 集成与交付

- [x] 3.1 检查新增中文文案的字体子集并按需更新既有生成输入，执行 Godot 导入检查及 Windows x86_64、macOS universal 导出；验证字体显示、导出成功和未增加依赖，明确无法实机运行的平台限制。
- [x] 3.2 使用真实账号和本地游戏服进行桌面渲染与连接联调，验证首次进入、恢复登录、断线重连、覆盖层隐藏/恢复及 RTT 动态变化；必要截图和过程输出仅留在 `.local/`，交付说明简述结果。
- [x] 3.3 将最终行为与规划文件核对，执行 `openspec validate add-player-connection-hud --strict` 与 `git diff --check`；检查文件清单后按明确路径暂存并创建 Conventional Commit，不混入无关变更。
