## 1. 实现
- [x] 1.1 核对现有照片覆盖和材质，创建工作分支。
- [x] 1.2 实现面砖数据图生成、导入、切线修正与石铺地微表面。
- [x] 1.3 更新校医院 77487 的 1 个合并面砖网格，保留模型其他节点；补充来源、参数、哈希与精度记录。

## 2. 验证与交付
- [x] 2.1 725 个网格顶点、法线、索引与变换保持一致，207 个碰撞标记网格及全部节点元数据不变。photo_surfaces 的缺口、非退化 UV、切线正交与方向检查通过。
- [x] 2.2 Forward+ 与 Mobile 均完成面砖、石铺地近远景渲染，检查修改前后图像；无本轮着色器错误。Mobile 使用桌面 GPU，未做 Android 真机或性能验收。
- [x] 2.3 Windows 独立 EXE、macOS ZIP、Linux 服务端可执行文件及 PCK 导出成功；专项检查确认三校区可加载、新增材质进入 Windows 包、服务端包不包含视觉资源。Windows EXE 启动退出码为 0，导出包在图形模式完成首页渲染。OpenSpec 严格校验通过。macOS 未进行本机运行、arm64 裁剪、严格签名与 DMG 验收。
- [x] 2.4 检查最终 diff，代码与最终资源分别提交。

已知验证限制：完整 desktop_export 测试在既有凌水植被为空的断言处失败；photo_road_details 的四次楼梯上下行通过，但花园连接处行走断言失败，使用修改前模型复现相同结果。两项均未作为本轮通过项，也未扩大范围改动植被或道路。无头编辑器导入仍报告既有 campus_backdrop.tscn 解析错误，图形模式导出与实际加载成功。未增加中文界面文案、依赖或导出根资源项。
