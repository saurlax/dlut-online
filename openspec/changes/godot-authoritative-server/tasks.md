## 1. 共享世界与运动

- [ ] 1.1 拆出共享运动规则，保持现有速度、跳跃、坡度与跌落复位；以现有运动检查及落地跳跃、按住不连跳验证。
- [ ] 1.2 提取校园碰撞生成器，从注册表、场景和碰撞标记生成服务端静态场景；验证三校区地面、墙、已存在通行空间、边界与客户端一致，未引入新室内或装饰碰撞。
- [ ] 1.3 新增独立无头入口和三校区隔离 World3D；验证同坐标跨校区不互相碰撞，入口不创建相机、UI、视觉模型或下载器。

## 2. Go 入场与网关

- [ ] 2.1 实现有界、30 秒有效的一次性游客票据签发及内部兑换；测试原子消费、过期、伪造、校区/版本校验、限流和不同凭据权限。
- [ ] 2.2 将 `/ws` 改为 Godot 反向代理，保留同源校验、PORT 和根地址语义，移除旧广播实现；用真实 Upgrade、跨源拒绝和现有静态资源测试验证。
- [ ] 2.3 增加简洁站点入口，文档列明当前 API；检查首页可进入 `/web/`、无虚构 SSO/持久化端点且资源缺失仍返回 404。

## 3. Godot 权威连接与协议

- [ ] 3.1 实现原生 WebSocket 服务端、异步票据兑换、50 人上限和连接代次；集成测试验证游客入场、无效票据、满员、同身份替换和旧回调隔离。
- [ ] 3.2 实现版本 2 输入、固定步模拟、序号和跳跃消费、暂停/超时停止与输入限制；用物理测试验证穿墙、速度伪造、泛洪、跳跃及旧版本拒绝。
- [ ] 3.3 实现每校区 10 Hz 快照复用、名册及有界慢连接处理；验证校区隔离、包含本机权威状态、停止读取客户端不会拖慢其他玩家或无限增长缓存。

## 4. 客户端迁移

- [ ] 4.1 用原生 HTTPRequest 获取入场票据并连接新协议，保留游客封面、重连与三校区传送；真实双客户端检查身份、名册、4001 停止重试及切校区角色清理。
- [ ] 4.2 接入输入发送、本地预测、权威纠正和远端插值；在 100–200 ms 往返延迟下实际检查移动、跳跃、墙边及坡道，确认篡改本地位置会被纠正。
- [ ] 4.3 保持暂停、失焦、地图和恢复行为，联网前/断线时停止移动；Web 实际操作验证并检查新增中文字体子集。

## 5. 在线查询与故障隔离

- [ ] 5.1 实现内部实例注册、5 秒完整在线上报、代次/序号验证及重启恢复；测试延迟旧包、重复注册、Godot/Go 分别重启和零人快照。
- [ ] 5.2 提供公开人数与受保护在线玩家查询；测试 15 秒过期、未初始化、null 与零区别、权限隔离、响应禁缓存及不暴露敏感信息。
- [ ] 5.3 实现游戏服 HTTP 超时、退避及最新上报合并；模拟内部 API 不可用而代理仍通，验证现有玩家继续移动、新入场不绕过验证、恢复后在线数据收敛。

## 6. 导出、部署与整体验收

- [ ] 6.1 增加 Linux Godot 专用服务端导出及独立镜像、本地双服务配置；验证源模型不复制、碰撞先生成、客户端无服务端代码/凭据、服务端无视觉资源依赖。
- [ ] 6.2 更新 CI 构建 Go/Web、Godot 服务端及 Windows/macOS 客户端；验证两个容器真实入场、在线人数、PORT 和整套 Web 资源，保持现有发布约定。
- [ ] 6.3 执行单校区 50 人持续移动至少 10 分钟，加入慢连接与断线恢复；记录运行机器、tick 耗时分位数、CPU、内存、出口及队列，无持续积压，p95 物理 tick 耗时低于 16.7 ms；结果放本地验收记录并简要交付，不预称生产容量已通过。
- [ ] 6.4 运行 Go 测试和 race 检查、Godot 物理/协议检查、Web 导出和浏览器实际渲染；验证三校区传送、原有地图交互和实际门洞/通行碰撞，记录未解决限制。
- [ ] 6.5 更新 AGENTS.md、README、Go 启动文档及旧游客同步设计的取代说明，说明 SSO/数据库/历史分析边界和协调回滚；执行 OpenSpec 严格校验与引用检查，逐项检查 diff 并按独立任务提交，不推送。
